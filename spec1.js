describe('Protractor baby steps', () => {
  // Test case
  it('Open Angular Website', () => {
    browser.get('https://angularjs.org');
  });

  // Test case
  if (('Close browser', () => {}));
});
